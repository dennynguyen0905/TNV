# Keeps the public/ directory in git.
# Static assets served at the site root live here. LOCAL_UPLOAD_DIR defaults to
# public/uploads (local storage driver placeholder — object storage is deferred).
